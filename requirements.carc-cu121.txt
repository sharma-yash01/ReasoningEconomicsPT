# CARC-specific requirements for CUDA 12.1 (Discovery cluster, A40 GPUs).
# Install with:
#   pip install -r requirements.carc-cu121.txt \
#       --extra-index-url https://download.pytorch.org/whl/cu121

# ---- Deep learning runtime (CUDA 12.1 wheels) ----
torch==2.8.*
numpy>=1.26

# ---- Inference / generation ----
vllm==0.10.2

# ---- Core training stack ----
openenv-core[core]==0.2.*
trl==1.0.0
jmespath>=1.0.1
transformers>=5.2.0
datasets>=4.7.0
accelerate>=1.4.0 

# ---- Data / config ----
pydantic>=2.11.7